{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "dev": "npm-run-all -p -r backend frontend",
    "heroku-postbuild": "npm-run-all -p -r backend frontend",
    "frontend": "cd feed-me-now && npm install && npm run start",
    "backend": "npm install && node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "body-parser": "^1.18.3",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "npm-run-all": "^4.1.5"
  }
}
